streamlit>=1.38
python-docx>=1.1
matplotlib>=3.8
pandas>=2.0
# Nécessaire uniquement pour la production (backend PostgreSQL, activé via la
# variable d'environnement DATABASE_URL) ; inutile pour l'usage SQLite par défaut.
psycopg2-binary>=2.9
